name: Build openexr

# IlmBase + OpenEXR, consumed by src/modeldoc_utils (Imath types for Alembic interop).
#
# sbox vendors 2.2.1 (2014), which does not compile on VS2022. The default here is the
# last 2.5.x release: still the IlmBase-era layout, so headers stay at <OpenEXR/...> and
# the Half/Iex/Imath/IlmThread/IlmImf split is unchanged. Do NOT jump to 3.x - that
# replaces IlmBase with Imath and moves headers to <Imath/...>, which is a source change
# in modeldoc_utils, not a dependency move.
#
# zlib is built from source on Windows only; the Linux and macOS runners have a usable
# system zlib.

on:
  workflow_dispatch:
    inputs:
      lib_version:
        description: 'OpenEXR tag (e.g. v2.5.8)'
        required: true
        default: 'v2.5.8'
      zlib_version:
        description: 'zlib tag to build against (Windows only)'
        required: true
        default: 'v1.3.1'
  push:
    tags: ['openexr-*']

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      lib_version: ${{ steps.ver.outputs.lib_version }}
      release_tag: ${{ steps.ver.outputs.release_tag }}
      zlib_version: ${{ steps.ver.outputs.zlib_version }}
    steps:
      - id: ver
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            V="${{ github.ref_name }}"; V="${V#openexr-}"
            Z="v1.3.1"
          else
            V="${{ inputs.lib_version }}"
            Z="${{ inputs.zlib_version }}"
          fi
          echo "lib_version=$V" >> $GITHUB_OUTPUT
          echo "zlib_version=$Z" >> $GITHUB_OUTPUT
          echo "release_tag=openexr-$V" >> $GITHUB_OUTPUT

  build:
    needs: setup
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: windows-latest,   platform: win64 }
          - { os: ubuntu-24.04,     platform: linuxsteamrt64, container: 'registry.gitlab.steamos.cloud/steamrt/sniper/sdk:3.0.20260805.254768' }
          - { os: ubuntu-24.04-arm, platform: linuxsteamrtarm64 }
          - { os: macos-26,         platform: osxarm64 }
    runs-on: ${{ matrix.os }}
    container: ${{ matrix.container }}
    name: ${{ matrix.platform }}

    steps:
      # linuxsteamrt64 builds run inside the Steam Runtime 3 (sniper) SDK so their output only
      # needs symbols the runtime actually has. Building on ubuntu-latest instead is what left
      # these libraries needing GLIBC_2.38 when sniper provides 2.31. The image carries neither
      # sudo nor git-lfs, which actions/checkout and the dep steps below expect.
      - name: Prepare Steam Runtime container
        if: matrix.container != ''
        shell: bash
        run: |
          apt-get update -qq
          apt-get install -y --no-install-recommends sudo git-lfs ca-certificates

      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-msvc
        if: runner.os == 'Windows'

      - name: Install cmake + ninja + zlib (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update -qq && sudo apt-get install -y cmake ninja-build zlib1g-dev

      - name: Install ninja (macOS)
        if: runner.os == 'macOS'
        run: brew install ninja

      # OpenEXR needs zlib and the Windows runner has none, so build a static /MT one.
      - name: Build zlib ${{ needs.setup.outputs.zlib_version }} (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          git clone --depth 1 --branch ${{ needs.setup.outputs.zlib_version }} https://github.com/madler/zlib zlib-src
          cmake -S zlib-src -B zlib-build -G Ninja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_INSTALL_PREFIX="$env:GITHUB_WORKSPACE\zlib-dist" `
            -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded `
            -DCMAKE_POLICY_DEFAULT_CMP0091=NEW
          cmake --build zlib-build --parallel
          cmake --install zlib-build

      - name: Clone OpenEXR ${{ needs.setup.outputs.lib_version }}
        run: git clone --depth 1 --branch ${{ needs.setup.outputs.lib_version }} https://github.com/AcademySoftwareFoundation/openexr src

      # -include cstdint: 2.5.x predates GCC 13, which stopped pulling <cstdint> in
      # transitively, so ImfHuf.cpp fails with "'uint64_t' was not declared in this scope".
      # Apple Clang still provides it, which is why osxarm64 passed without this.
      - name: Build (Unix)
        if: runner.os != 'Windows'
        run: |
          cmake -S src -B src/build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/dist" \
            -DCMAKE_POSITION_INDEPENDENT_CODE=ON \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
            -DCMAKE_CXX_FLAGS="-include cstdint" \
            -DBUILD_SHARED_LIBS=OFF \
            -DBUILD_TESTING=OFF \
            -DOPENEXR_BUILD_UTILS=OFF \
            -DOPENEXR_VIEWERS_ENABLE=OFF \
            -DPYILMBASE_ENABLE=OFF \
            -DOPENEXR_INSTALL_EXAMPLES=OFF
          cmake --build src/build --parallel
          cmake --install src/build

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          cmake -S src -B src\build -G Ninja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_INSTALL_PREFIX="$env:GITHUB_WORKSPACE\dist" `
            -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded `
            -DCMAKE_POLICY_DEFAULT_CMP0091=NEW `
            "-DCMAKE_POLICY_VERSION_MINIMUM=3.5" `
            -DCMAKE_PREFIX_PATH="$env:GITHUB_WORKSPACE\zlib-dist" `
            -DZLIB_ROOT="$env:GITHUB_WORKSPACE\zlib-dist" `
            -DZLIB_USE_STATIC_LIBS=ON `
            -DBUILD_SHARED_LIBS=OFF `
            -DBUILD_TESTING=OFF `
            -DOPENEXR_BUILD_UTILS=OFF `
            -DOPENEXR_VIEWERS_ENABLE=OFF `
            -DPYILMBASE_ENABLE=OFF `
            -DOPENEXR_INSTALL_EXAMPLES=OFF
          cmake --build src\build --parallel
          cmake --install src\build

      - uses: ./.github/actions/collect-package-upload
        with:
          lib_name: openexr
          lib_version: ${{ needs.setup.outputs.lib_version }}
          platform: ${{ matrix.platform }}

  release:
    needs: [setup, build]
    uses: ./.github/workflows/_release.yml
    with:
      release_tag: ${{ needs.setup.outputs.release_tag }}
      display_name: "openexr ${{ needs.setup.outputs.lib_version }}"
