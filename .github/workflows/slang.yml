name: Build slang

# Builds the Slang shader compiler as shared libraries (slang, slang-rt, slang-glslang,
# slang-glsl-module). The LLVM flavor is disabled so the build does not pull down a
# prebuilt slang-llvm; Source 2 only uses the SPIR-V/GLSL paths.

on:
  workflow_dispatch:
    inputs:
      lib_version:
        description: 'slang tag (e.g. v2026.14)'
        required: true
        default: 'v2026.14'
  push:
    tags: ['slang-*']

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      lib_version: ${{ steps.ver.outputs.lib_version }}
      release_tag: ${{ steps.ver.outputs.release_tag }}
    steps:
      - id: ver
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            V="${{ github.ref_name }}"; V="${V#slang-}"
          else
            V="${{ inputs.lib_version }}"
          fi
          echo "lib_version=$V" >> $GITHUB_OUTPUT
          echo "release_tag=slang-$V" >> $GITHUB_OUTPUT

  build:
    needs: setup
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: windows-latest,   platform: win64 }
          - { os: ubuntu-24.04,     platform: linuxsteamrt64, container: 'registry.gitlab.steamos.cloud/steamrt/sniper/sdk:3.0.20260805.254768' }
          - { os: ubuntu-24.04-arm, platform: linuxsteamrtarm64 }
          - { os: macos-26,         platform: osxarm64 }
    runs-on: ${{ matrix.os }}
    container: ${{ matrix.container }}
    name: ${{ matrix.platform }}

    steps:
      # linuxsteamrt64 builds run inside the Steam Runtime 3 (sniper) SDK so their output only
      # needs symbols the runtime actually has. Building on ubuntu-latest instead is what left
      # these libraries needing GLIBC_2.38 when sniper provides 2.31. The image carries neither
      # sudo nor git-lfs, which actions/checkout and the dep steps below expect.
      - name: Prepare Steam Runtime container
        if: matrix.container != ''
        shell: bash
        run: |
          apt-get update -qq
          apt-get install -y --no-install-recommends sudo git-lfs ca-certificates

      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-msvc
        if: runner.os == 'Windows'

      # X11 headers are needed even with gfx off: slang-rhi's vendored vulkan.h pulls in
      # X11/Xlib.h on Linux. SLANG_ENABLE_SLANG_RHI=OFF below should skip it, but keep the
      # headers so the build does not hinge on that option name staying stable.
      - name: Install cmake + ninja (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y cmake ninja-build \
            libx11-dev libxrandr-dev libxext-dev libxcb1-dev

      - name: Install ninja (macOS)
        if: runner.os == 'macOS'
        run: brew install ninja

      # setup-python resolves its download against the runner's distro and publishes no build
      # for steamrt, so it is skipped inside the container. sniper ships Python 3.9 as both
      # python and python3, which is what these build scripts want it for.
      - uses: actions/setup-python@v5
        if: matrix.container == ''
        with:
          python-version: '3.12'

      - name: Clone slang ${{ needs.setup.outputs.lib_version }}
        run: git clone --depth 1 --recurse-submodules --shallow-submodules --branch ${{ needs.setup.outputs.lib_version }} https://github.com/shader-slang/slang src

      - name: Build (Unix)
        if: runner.os != 'Windows'
        run: |
          cmake -S src -B src/build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/dist" \
            -DSLANG_SLANG_LLVM_FLAVOR=DISABLE \
            -DSLANG_ENABLE_TESTS=OFF \
            -DSLANG_ENABLE_EXAMPLES=OFF \
            -DSLANG_ENABLE_GFX=OFF \
            -DSLANG_ENABLE_SLANG_RHI=OFF \
            -DSLANG_ENABLE_SLANGD=OFF \
            -DSLANG_ENABLE_SLANGRT=ON \
            -DSLANG_LIB_TYPE=SHARED
          cmake --build src/build --parallel
          cmake --install src/build

      # Static CRT even though this ships as a DLL, so sbox needs no VC redist.
      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          cmake -S src -B src\build -G Ninja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_INSTALL_PREFIX="$env:GITHUB_WORKSPACE\dist" `
            -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded `
            -DSLANG_SLANG_LLVM_FLAVOR=DISABLE `
            -DSLANG_ENABLE_TESTS=OFF `
            -DSLANG_ENABLE_EXAMPLES=OFF `
            -DSLANG_ENABLE_GFX=OFF `
            -DSLANG_ENABLE_SLANG_RHI=OFF `
            -DSLANG_ENABLE_SLANGD=OFF `
            -DSLANG_ENABLE_SLANGRT=ON `
            -DSLANG_LIB_TYPE=SHARED
          cmake --build src\build --parallel
          cmake --install src\build

      - uses: ./.github/actions/collect-package-upload
        with:
          lib_name: slang
          lib_version: ${{ needs.setup.outputs.lib_version }}
          platform: ${{ matrix.platform }}

  release:
    needs: [setup, build]
    uses: ./.github/workflows/_release.yml
    with:
      release_tag: ${{ needs.setup.outputs.release_tag }}
      display_name: "slang ${{ needs.setup.outputs.lib_version }}"
